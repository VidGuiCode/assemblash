{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Document",
  "description": "A whole document: canvas, imported assets, and the layer stack.",
  "type": "object",
  "properties": {
    "schemaVersion": {
      "description": "Schema version of this document, independent of the release version.",
      "type": "integer",
      "format": "uint32",
      "minimum": 0
    },
    "id": {
      "description": "Stable id, `doc_<ULID>`.",
      "$ref": "#/$defs/DocumentId"
    },
    "version": {
      "description": "How many mutations this document has had.\n\nA caller that read the document at version 7 sends 7 back with its\nnext mutation; if the document has moved on, the mutation is refused\nrather than silently overwriting someone else's work (PRD §10.3).",
      "type": "integer",
      "format": "uint64",
      "minimum": 0,
      "default": 0
    },
    "name": {
      "description": "Human-facing name. Not an identifier.",
      "type": [
        "string",
        "null"
      ]
    },
    "canvas": {
      "description": "Canvas the layers are composed on.",
      "$ref": "#/$defs/Canvas"
    },
    "assets": {
      "description": "Assets imported into the project, referenced by image layers.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/Asset"
      },
      "default": []
    },
    "layers": {
      "description": "Layers, bottom first: array order is z-order.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/Layer"
      },
      "default": []
    },
    "presets": {
      "description": "Named style bundles this document offers.\n\nIn the document rather than beside the workspace, so that a project\ndirectory stays portable and the same document cannot render\ndifferently depending on what else is installed next to it — see\n[`crate::presets`]. Additive with a default, like every field added\nsince schema version 1.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/Preset"
      }
    },
    "slots": {
      "description": "Named openings a caller may fill, making this document a template\n(PRD use case C). Empty for an ordinary document.\n\nAdditive with a default, like every other field added since schema\nversion 1: a build that does not know about slots preserves them, and\na document without them loads here unchanged.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/Slot"
      }
    }
  },
  "required": [
    "schemaVersion",
    "id",
    "canvas"
  ],
  "additionalProperties": true,
  "$defs": {
    "DocumentId": {
      "description": "Identifier of a document.",
      "type": "string"
    },
    "Canvas": {
      "description": "The fixed-size surface layers are composed on.",
      "type": "object",
      "properties": {
        "width": {
          "description": "Width in pixels; must be positive and finite.",
          "type": "number",
          "format": "double"
        },
        "height": {
          "description": "Height in pixels; must be positive and finite.",
          "type": "number",
          "format": "double"
        },
        "background": {
          "description": "Background fill. `None` means transparent.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "width",
        "height"
      ],
      "additionalProperties": true
    },
    "Color": {
      "description": "An sRGB colour, `#rrggbb` or `#rrggbbaa`.\n\nStored as written so a document round-trips exactly; validation checks the\nshape and [`Color::to_rgba`] parses it.",
      "type": "string"
    },
    "Asset": {
      "description": "An imported file living under the project's `assets/` directory.",
      "type": "object",
      "properties": {
        "id": {
          "description": "Stable id, `asset_<ULID>`.",
          "$ref": "#/$defs/AssetId"
        },
        "path": {
          "description": "Path relative to the project's `assets/` directory, `/`-separated.",
          "type": "string"
        },
        "hash": {
          "description": "Content hash of the file, `sha256:<hex>`. Detects silent edits.",
          "type": "string"
        },
        "mediaType": {
          "description": "Media type, e.g. `image/png`.",
          "type": "string"
        },
        "width": {
          "description": "Pixel width, when known.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "height": {
          "description": "Pixel height, when known.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "id",
        "path",
        "hash",
        "mediaType"
      ],
      "additionalProperties": true
    },
    "AssetId": {
      "description": "Identifier of an imported asset.",
      "type": "string"
    },
    "Layer": {
      "description": "One layer: the properties every layer has, plus its kind-specific payload.",
      "type": "object",
      "properties": {
        "id": {
          "description": "Stable id, `layer_<ULID>`.",
          "$ref": "#/$defs/LayerId"
        },
        "name": {
          "description": "Human-facing name. Not an identifier.",
          "type": [
            "string",
            "null"
          ]
        },
        "transform": {
          "description": "Placement in the parent's coordinate space.",
          "$ref": "#/$defs/Transform"
        },
        "opacity": {
          "description": "Opacity from 0 (invisible) to 1 (opaque).",
          "type": "number",
          "format": "double",
          "default": 1.0
        },
        "visible": {
          "description": "Whether the layer is rendered at all.",
          "type": "boolean",
          "default": true
        },
        "locked": {
          "description": "Whether editing tools should refuse to move this layer.\n\nThe user-facing \"don't let me nudge this by accident\" flag. An\nexplicit override can still change it (PRD §10.2).",
          "type": "boolean",
          "default": false
        },
        "protected": {
          "description": "Whether AI adapters and agents may change this layer at all.\n\nUnlike `locked`, there is no override: a protected layer is refused\nfor every mutation, whoever asks (PRD §10.2, MVP criterion 11).",
          "type": "boolean",
          "default": false
        },
        "readOnly": {
          "description": "Whether the layer is inspectable but never mutable through the API.",
          "type": "boolean",
          "default": false
        },
        "blendMode": {
          "description": "How this layer composites with what is underneath it.\n\nFourteen modes are drawn. `color-dodge` and `color-burn` are\ndeliberately not among them: they are not bit-identical on every\ntarget, and a mode that breaks determinism on one machine is worse\nthan a mode that says no. Those two, and any mode a newer build\nwrites, round-trip as written and are refused when something tries to\ndraw them, rather than being silently composited as `normal`.",
          "$ref": "#/$defs/BlendMode",
          "default": "normal"
        },
        "effects": {
          "description": "Adjustments applied to this layer when it is drawn, in order.\n\n**Never baked.** The document keeps the numbers and the pixels are\nderived from them every render, so an effect is as reversible as any\nother property and a layer under three effects is still the layer.\n\nAlways written, even when empty, exactly as it has been since schema\nversion 1: omitting it would change the bytes of every document that\nhas one, for no gain.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Effect"
          },
          "default": []
        },
        "constraints": {
          "description": "Reserved (layout constraints): preserved verbatim, never interpreted."
        },
        "clip": {
          "description": "The mask that hides everything the layer draws outside its box.\n\nThe clip geometry is the layer's transform box in its parent's\ncoordinate space, at rest: rotation and flips apply to the *clipped*\nresult, so a shadow carried by the layer follows the clipped\nsilhouette (the 1.8.0 composition spike measured this — the clip must\nsit inside the filter, not beside it). `None` draws everything, as\nevery build before 1.8.0 did.",
          "anyOf": [
            {
              "$ref": "#/$defs/Clip"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "id",
        "transform"
      ],
      "oneOf": [
        {
          "description": "A run of text in a single style.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "text"
            }
          },
          "$ref": "#/$defs/TextLayer",
          "required": [
            "type"
          ]
        },
        {
          "description": "An imported image.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "image"
            }
          },
          "$ref": "#/$defs/ImageLayer",
          "required": [
            "type"
          ]
        },
        {
          "description": "A container transforming its children as a unit.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "group"
            }
          },
          "$ref": "#/$defs/GroupLayer",
          "required": [
            "type"
          ]
        },
        {
          "description": "An imported vector graphic, drawn into the layer box.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "svg"
            }
          },
          "$ref": "#/$defs/SvgLayer",
          "required": [
            "type"
          ]
        },
        {
          "description": "A rectangle, an ellipse or a line, drawn from the document itself.",
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "const": "shape"
            }
          },
          "$ref": "#/$defs/ShapeLayer",
          "required": [
            "type"
          ]
        }
      ]
    },
    "LayerId": {
      "description": "Identifier of a layer.",
      "type": "string"
    },
    "Transform": {
      "description": "Position, size, and rotation of a layer in its parent's coordinate space.",
      "type": "object",
      "properties": {
        "x": {
          "description": "Left edge.",
          "type": "number",
          "format": "double"
        },
        "y": {
          "description": "Top edge.",
          "type": "number",
          "format": "double"
        },
        "width": {
          "description": "Box width; must be finite and not negative.",
          "type": "number",
          "format": "double"
        },
        "height": {
          "description": "Box height; must be finite and not negative.",
          "type": "number",
          "format": "double"
        },
        "rotation": {
          "description": "Clockwise rotation in degrees about the box centre.",
          "type": "number",
          "format": "double",
          "default": 0.0
        },
        "flipHorizontal": {
          "description": "Whether the content mirrors left-to-right about the box centre.\n\nWritten only when set, so no document from before 1.8.0 changes a\nbyte. A flip is a mirror, never a negative size: validation forbids\nnegative box sizes, and a negative size would be a second way of\nsaying where the layer's edges are.",
          "type": "boolean"
        },
        "flipVertical": {
          "description": "Whether the content mirrors top-to-bottom about the box centre.",
          "type": "boolean"
        }
      },
      "required": [
        "x",
        "y",
        "width",
        "height"
      ],
      "additionalProperties": true
    },
    "BlendMode": {
      "description": "How a layer composites onto what is beneath it.\n\nThe whole CSS separable-and-non-separable set, every one of which was\nchecked to rasterize before it was named here — a mode that only\nround-trips would be a promise the pixels do not keep.\n\n[`BlendMode::Other`] is what a mode written by some newer build becomes:\npreserved verbatim, because losing it would mean a document came back\ndamaged, but **refused at render time** rather than quietly composited as\n`normal`. Silently drawing the wrong thing is the worse failure: it looks\nlike it worked.",
      "anyOf": [
        {
          "description": "Plain source-over compositing.",
          "type": "string",
          "const": "normal"
        },
        {
          "description": "Darkens: multiplies the two colours.",
          "type": "string",
          "const": "multiply"
        },
        {
          "description": "Lightens: the inverse of multiplying the inverses.",
          "type": "string",
          "const": "screen"
        },
        {
          "description": "Multiplies or screens, depending on the backdrop.",
          "type": "string",
          "const": "overlay"
        },
        {
          "description": "Keeps the darker of the two.",
          "type": "string",
          "const": "darken"
        },
        {
          "description": "Keeps the lighter of the two.",
          "type": "string",
          "const": "lighten"
        },
        {
          "description": "Brightens the backdrop to reflect the source.",
          "type": "string",
          "const": "color-dodge"
        },
        {
          "description": "Darkens the backdrop to reflect the source.",
          "type": "string",
          "const": "color-burn"
        },
        {
          "description": "`Overlay` with the layers swapped.",
          "type": "string",
          "const": "hard-light"
        },
        {
          "description": "A softer `HardLight`.",
          "type": "string",
          "const": "soft-light"
        },
        {
          "description": "The absolute difference of the two.",
          "type": "string",
          "const": "difference"
        },
        {
          "description": "Like `Difference`, with less contrast.",
          "type": "string",
          "const": "exclusion"
        },
        {
          "description": "The source's hue, the backdrop's saturation and luminosity.",
          "type": "string",
          "const": "hue"
        },
        {
          "description": "The source's saturation, the backdrop's hue and luminosity.",
          "type": "string",
          "const": "saturation"
        },
        {
          "description": "The source's hue and saturation, the backdrop's luminosity.",
          "type": "string",
          "const": "color"
        },
        {
          "description": "The source's luminosity, the backdrop's hue and saturation.",
          "type": "string",
          "const": "luminosity"
        },
        {
          "description": "A mode this build does not render, preserved as written and refused\nwhen something tries to draw it.",
          "type": "string"
        }
      ]
    },
    "Effect": {
      "description": "One adjustment in a layer's effect stack.\n\nTagged by `type`, so the JSON reads as what it is. [`Effect::Other`] keeps\nan effect written by a newer build verbatim and refuses to render it —\nthe same bargain as [`BlendMode::Other`]: never lose it, never guess at it.\n\nThe amounts are multipliers where 1 means \"unchanged\", which is what\n`filter: brightness(1.2)` means everywhere else, so a number copied from a\nCSS example does what it looks like it does.\n\nEvery known variant also carries a `#[serde(flatten)]` capture map\n(D27): a key this build does not know rides along verbatim instead of\nbeing dropped on the next save.",
      "anyOf": [
        {
          "description": "Scales each channel. 1 is unchanged, 0 is black.",
          "type": "object",
          "properties": {
            "amount": {
              "description": "The multiplier.",
              "type": "number",
              "format": "double"
            },
            "type": {
              "type": "string",
              "const": "brightness"
            }
          },
          "required": [
            "type",
            "amount"
          ],
          "additionalProperties": true
        },
        {
          "description": "Pushes each channel away from mid grey. 1 is unchanged, 0 is flat grey.",
          "type": "object",
          "properties": {
            "amount": {
              "description": "The multiplier.",
              "type": "number",
              "format": "double"
            },
            "type": {
              "type": "string",
              "const": "contrast"
            }
          },
          "required": [
            "type",
            "amount"
          ],
          "additionalProperties": true
        },
        {
          "description": "Scales colourfulness. 1 is unchanged, 0 is greyscale.",
          "type": "object",
          "properties": {
            "amount": {
              "description": "The multiplier.",
              "type": "number",
              "format": "double"
            },
            "type": {
              "type": "string",
              "const": "saturation"
            }
          },
          "required": [
            "type",
            "amount"
          ],
          "additionalProperties": true
        },
        {
          "description": "A Gaussian blur.",
          "type": "object",
          "properties": {
            "radius": {
              "description": "Standard deviation, in document units. 0 does nothing.",
              "type": "number",
              "format": "double"
            },
            "type": {
              "type": "string",
              "const": "blur"
            }
          },
          "required": [
            "type",
            "radius"
          ],
          "additionalProperties": true
        },
        {
          "description": "Seeded monochrome noise, multiplied over the layer.\n\nThe seed is part of the document, not the run: the same document\nproduces the same grain on every machine and in every render (NFR-3).\nGrain from a clock or a random number generator would quietly break\nthe one property this project is built on.",
          "type": "object",
          "properties": {
            "amount": {
              "description": "How far the noise swings either side of unchanged, 0 to 1.",
              "type": "number",
              "format": "double"
            },
            "seed": {
              "description": "The noise seed.",
              "type": "integer",
              "format": "uint32",
              "minimum": 0
            },
            "scale": {
              "description": "Size of the noise features; 1 is fine grain, larger is coarser.",
              "type": "number",
              "format": "double",
              "default": 1.0
            },
            "type": {
              "type": "string",
              "const": "grain"
            }
          },
          "required": [
            "type",
            "amount",
            "seed"
          ],
          "additionalProperties": true
        },
        {
          "description": "A soft offset copy of the layer's alpha, drawn beneath it.\n\nA glow is this with `dx` and `dy` at 0, and Lift is this with a small\noffset and a soft blur. There is no second primitive for either: a\ncentred shadow *is* a glow, and two names for one filter would be two\nthings to keep bit-identical for no gain.",
          "type": "object",
          "properties": {
            "dx": {
              "description": "Horizontal offset in document units.\n\nA fractional offset is resampled by the filter and reads slightly\nsofter; whole numbers give the crispest edge.",
              "type": "number",
              "format": "double"
            },
            "dy": {
              "description": "Vertical offset in document units. See `dx`.",
              "type": "number",
              "format": "double"
            },
            "blur": {
              "description": "Standard deviation of the blur, 0 or more. 0 is a hard-edged\noffset copy.",
              "type": "number",
              "format": "double"
            },
            "color": {
              "description": "Shadow colour. An `#rrggbbaa` alpha becomes the flood opacity, so\na shadow's strength is written where every other colour writes it.",
              "$ref": "#/$defs/Color"
            },
            "type": {
              "type": "string",
              "const": "dropShadow"
            }
          },
          "required": [
            "type",
            "dx",
            "dy",
            "blur",
            "color"
          ],
          "additionalProperties": true
        },
        {
          "description": "An effect this build does not know, preserved as written and refused\nwhen something tries to draw it."
        }
      ]
    },
    "Clip": {
      "description": "The mask shape of a layer's [`Layer::clip`], tagged by `\"shape\"` in JSON.\n\nThe geometry always is the layer's transform box, so the variants carry no\ncoordinates — only what the box alone does not say (a path's `d` sits in\nthe box, scaled by it, once the renderer draws it). Every known variant\nalso carries a `#[serde(flatten)]` capture map (D27): a key this build\ndoes not know rides along verbatim instead of being dropped.\n[`Clip::Other`] is an untagged catch-all (D21): a clip written by a newer\nbuild is preserved as written, refused when an update touches it, and\nrefused at render time — the same bargain as [`ShapeKind::Other`] (and,\nlike it, never flattened into its parent, so the catch-all's reach stays\nwith the clip).",
      "anyOf": [
        {
          "description": "A rounded rectangle filling the transform box.",
          "type": "object",
          "properties": {
            "cornerRadius": {
              "description": "Corner radius in document units; 0 is a square corner.\n\nClamped to half the shorter side when it is drawn, exactly like a\nshape rect's radius — a large radius makes a stadium, not an\nerror.",
              "type": "number",
              "format": "double",
              "default": 0.0
            },
            "shape": {
              "type": "string",
              "const": "rect"
            }
          },
          "additionalProperties": true,
          "required": [
            "shape"
          ]
        },
        {
          "description": "An ellipse inscribed in the transform box — the circle avatar.",
          "type": "object",
          "properties": {
            "shape": {
              "type": "string",
              "const": "ellipse"
            }
          },
          "additionalProperties": true,
          "required": [
            "shape"
          ]
        },
        {
          "description": "A path filling the transform box, closed, in the conservative grammar\nof [`crate::path`]. The `d` string is validated at operation time; a\n`d` that fails the grammar cannot be stored through this build.",
          "type": "object",
          "properties": {
            "d": {
              "description": "The path data, as written in an SVG `d` attribute. Validated\nagainst [`crate::path`] at operation time.",
              "type": "string"
            },
            "shape": {
              "type": "string",
              "const": "path"
            }
          },
          "required": [
            "shape",
            "d"
          ],
          "additionalProperties": true
        },
        {
          "description": "A clip this build does not know, preserved as written, refused when an\nupdate touches it, and refused when something tries to draw it (D21)."
        }
      ]
    },
    "TextAlign": {
      "description": "Horizontal text alignment inside the layer box.",
      "oneOf": [
        {
          "description": "Aligned to the left edge.",
          "type": "string",
          "const": "left"
        },
        {
          "description": "Centred horizontally.",
          "type": "string",
          "const": "center"
        },
        {
          "description": "Aligned to the right edge.",
          "type": "string",
          "const": "right"
        }
      ]
    },
    "FontStyle": {
      "description": "Whether glyphs stand upright or slant.",
      "oneOf": [
        {
          "description": "The upright face.",
          "type": "string",
          "const": "normal"
        },
        {
          "description": "The italic face (an oblique stands in when the family has no true\nitalic — the font set still keys it as italic).",
          "type": "string",
          "const": "italic"
        }
      ]
    },
    "Stroke": {
      "description": "A shape's edge paint.\n\nLike every payload in the document, a stroke also carries a\n`#[serde(flatten)]` capture map (D27): a key this build does not know\nrides along verbatim instead of being dropped on the next save.",
      "type": "object",
      "properties": {
        "color": {
          "description": "Edge colour.",
          "$ref": "#/$defs/Color"
        },
        "width": {
          "description": "Width in document units; must be finite and 0 or more.\n\n**Painted inset** on a rect and an ellipse (D5): the stroke grows\ninward from the transform box, so the box is the visual box and\nnothing that reasons about layout has to know whether a shape is\nstroked. A line has no interior, so its stroke is centred on the\nsegment instead — half of it falls either side of the box's middle.\n\nOne caveat, measured rather than assumed: a width below one device\npixel is drawn as a *hairline* centred on the geometric edge, and a\nhairline occupies a whole pixel row. A 0.5-wide stroke therefore\nspills up to half a pixel outside the box on each side. It is\ndeterministic and identical on every target, so it is documented here\nrather than refused — but at that width the box is not quite the\nvisual box.",
          "type": "number",
          "format": "double"
        },
        "dashArray": {
          "description": "The dash pattern, alternating paint and gap in document units,\nstarting with paint. `None` is a solid line.\n\nWhen present: non-empty, at most [`MAX_DASH_ENTRIES`] entries, every\nvalue finite and greater than 0 — validated at operation time, where\nevery stroke limit lives. A pattern this build cannot draw is a\nrefusal at the moment it is set, not a surprise at render time.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "number",
            "format": "double"
          }
        },
        "lineCap": {
          "description": "How the stroke ends, when the stroke is set. `None` means the\nrenderer's default.",
          "anyOf": [
            {
              "$ref": "#/$defs/LineCap"
            },
            {
              "type": "null"
            }
          ]
        },
        "lineJoin": {
          "description": "How two segments of the stroke meet, when the stroke is set. `None`\nmeans the renderer's default.",
          "anyOf": [
            {
              "$ref": "#/$defs/LineJoin"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "color",
        "width"
      ],
      "additionalProperties": true
    },
    "LineCap": {
      "description": "How a stroke ends.\n\nA fixed, named set, with an untagged [`LineCap::Other`] catch-all (D21):\na value written by a newer build round-trips verbatim and is refused when\nsomething tries to draw it — never guessed at and never lost.",
      "anyOf": [
        {
          "description": "Flat, cut off at the endpoint.",
          "type": "string",
          "const": "butt"
        },
        {
          "description": "A half-disc past the endpoint.",
          "type": "string",
          "const": "round"
        },
        {
          "description": "A square half a width past the endpoint.",
          "type": "string",
          "const": "square"
        },
        {
          "description": "A cap this build does not know, preserved verbatim (D21)."
        }
      ]
    },
    "LineJoin": {
      "description": "How two segments of a stroke meet at a corner.\n\nThe same fixed set and catch-all bargain as [`LineCap`].",
      "anyOf": [
        {
          "description": "Sharp corner, extended to the stroke width.",
          "type": "string",
          "const": "miter"
        },
        {
          "description": "Rounded corner.",
          "type": "string",
          "const": "round"
        },
        {
          "description": "Bevelled corner, cut across.",
          "type": "string",
          "const": "bevel"
        },
        {
          "description": "A join this build does not know, preserved verbatim (D21)."
        }
      ]
    },
    "VerticalAlign": {
      "description": "Where the text block sits vertically in the layer box.",
      "oneOf": [
        {
          "description": "The first baseline one ascent below the box top — the behaviour every\nbuild up to 1.6 had, and the default so nothing moves.",
          "type": "string",
          "const": "top"
        },
        {
          "description": "The block centred in the box's height.",
          "type": "string",
          "const": "middle"
        },
        {
          "description": "The block's last line resting on the box bottom.",
          "type": "string",
          "const": "bottom"
        }
      ]
    },
    "TextLayer": {
      "description": "Text content and its single style. Per-run styling arrives in v2.0.",
      "type": "object",
      "properties": {
        "text": {
          "description": "The text. `\\n` starts a new line.",
          "type": "string"
        },
        "fontFamily": {
          "description": "Font family name, resolved against the caller's font set.",
          "type": "string"
        },
        "fontSize": {
          "description": "Font size in pixels; must be positive and finite.",
          "type": "number",
          "format": "double"
        },
        "color": {
          "description": "Fill colour. `None` means no fill — SVG's `fill=\"none\"`, the same\nbargain as a shape's. A stroke-only text layer is the hollow style.\n\n`None` serializes as `null`, never as an omitted key: a round trip\nthrough a build that omits the key has to come back as the same\nlayer, and an absent key means the pre-1.7 default of black.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color"
            },
            {
              "type": "null"
            }
          ],
          "default": "#000000"
        },
        "align": {
          "description": "Horizontal alignment within the layer box.",
          "$ref": "#/$defs/TextAlign",
          "default": "left"
        },
        "lineHeight": {
          "description": "Line height as a multiple of the font size.",
          "type": "number",
          "format": "double",
          "default": 1.2
        },
        "fontWeight": {
          "description": "Font weight, 100 to 900 as CSS names them.\n\nResolved against the caller's font set by exact face: a family whose\nbold face is not loaded is a typed refusal at render time, not a\nsilent nearest match. 400 is the regular face.",
          "type": "integer",
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "fontStyle": {
          "description": "Upright or italic, resolved with the weight.",
          "$ref": "#/$defs/FontStyle"
        },
        "letterSpacing": {
          "description": "Extra space between characters, in pixels.\n\nPart of measurement, not just of drawing: wrapping, the text-layout\nendpoint and the export all add it per gap, so a line measured here is\nthe line that renders.",
          "type": "number",
          "format": "double"
        },
        "stroke": {
          "description": "Glyph stroke, painted centred on the outline with\n`paint-order=\"stroke\"` — the fill goes down first, so a stroke never\neats the letter (D5, text half). `None` means no stroke.",
          "anyOf": [
            {
              "$ref": "#/$defs/Stroke"
            },
            {
              "type": "null"
            }
          ]
        },
        "verticalAlign": {
          "description": "Where the text block sits vertically in the layer box.\n\n`top` is the only behaviour every earlier build had — the baseline\nfixed one ascent below the box top — so it is the default and existing\ndocuments render pixel-identically (D19).",
          "$ref": "#/$defs/VerticalAlign"
        },
        "runs": {
          "description": "Reserved (v2.0 styled runs): preserved verbatim, never interpreted.",
          "type": "array",
          "items": true,
          "default": []
        }
      },
      "required": [
        "text",
        "fontFamily",
        "fontSize"
      ],
      "additionalProperties": true
    },
    "ImageFit": {
      "description": "How an image is scaled into its box.",
      "oneOf": [
        {
          "description": "Stretch to the box, ignoring aspect ratio.",
          "type": "string",
          "const": "fill"
        },
        {
          "description": "Scale down to fit inside the box, keeping aspect ratio.",
          "type": "string",
          "const": "contain"
        },
        {
          "description": "Scale to cover the box, keeping aspect ratio, cropping the overflow.",
          "type": "string",
          "const": "cover"
        }
      ]
    },
    "Crop": {
      "description": "A rectangle in an image's source pixel space.\n\nA document type of its own, not [`crate::layout::Rect`]: that one is an\ninternal `Copy` type in absolute canvas space, and a crop is none of\nthose things — it serialises, and it is relative to the asset.",
      "type": "object",
      "properties": {
        "x": {
          "description": "Left edge in source pixels.",
          "type": "number",
          "format": "double"
        },
        "y": {
          "description": "Top edge in source pixels.",
          "type": "number",
          "format": "double"
        },
        "width": {
          "description": "Width in source pixels; must be positive and finite.",
          "type": "number",
          "format": "double"
        },
        "height": {
          "description": "Height in source pixels; must be positive and finite.",
          "type": "number",
          "format": "double"
        }
      },
      "required": [
        "x",
        "y",
        "width",
        "height"
      ],
      "additionalProperties": true
    },
    "ImageLayer": {
      "description": "A reference to an imported asset, drawn into the layer box.",
      "type": "object",
      "properties": {
        "asset": {
          "description": "Id of an asset in the document's `assets` list.",
          "$ref": "#/$defs/AssetId"
        },
        "fit": {
          "description": "How the image fills its box.",
          "$ref": "#/$defs/ImageFit",
          "default": "fill"
        },
        "crop": {
          "description": "The sub-rectangle of the source image that fills the box, in source\npixels — a crop is a rectangle, not a fit mode (v1.8.0).\n\nIt composes with [`ImageLayer::fit`]: the sub-rectangle is placed into\nthe box as if it were the whole image, so `fill` stretches it and\n`contain`/`cover` work from its aspect. `None` uses the whole image,\nas every build before 1.8.0 did. Out-of-bounds rectangles clamp to the\nsource when drawn; a crop with no intersection with the source is a\ntyped render refusal, never zero ink.",
          "anyOf": [
            {
              "$ref": "#/$defs/Crop"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "asset"
      ],
      "additionalProperties": true
    },
    "GroupLayer": {
      "description": "A group of layers, transformed as a unit.",
      "type": "object",
      "properties": {
        "children": {
          "description": "Children, bottom first, positioned relative to the group.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Layer"
          },
          "default": []
        }
      },
      "additionalProperties": true
    },
    "SvgLayer": {
      "description": "A reference to an imported SVG asset, drawn into the layer box.\n\nSeparate from [`ImageLayer`] because an SVG is vector: it scales without\nloss, and it went through the import sanitiser (`crate::svg_import`) before\nit was stored. Nothing in a project's `assets/` directory carries scripts\nor external references.",
      "type": "object",
      "properties": {
        "asset": {
          "description": "Id of an asset in the document's `assets` list.",
          "$ref": "#/$defs/AssetId"
        },
        "fit": {
          "description": "How the graphic fills its box.",
          "$ref": "#/$defs/ImageFit",
          "default": "fill"
        }
      },
      "required": [
        "asset"
      ],
      "additionalProperties": true
    },
    "ShapeKind": {
      "description": "The geometry of a [`ShapeLayer`], tagged by `\"kind\"` in JSON.\n\nEvery known variant also carries a `#[serde(flatten)]` capture map (D27):\na key this build does not know rides along verbatim instead of being\ndropped. The unit variants became struct variants holding only the map,\nwhich leaves their serialized form byte-identical (the D27 spike measured\nit: `{\"kind\":\"ellipse\"}` stays `{\"kind\":\"ellipse\"}`).",
      "anyOf": [
        {
          "description": "A rectangle filling the transform box.",
          "type": "object",
          "properties": {
            "cornerRadius": {
              "description": "Corner radius in document units; 0 is a square corner.\n\nClamped to half the shorter side when it is drawn, so a radius\nlarger than the box makes a stadium rather than a refusal.",
              "type": "number",
              "format": "double",
              "default": 0.0
            },
            "kind": {
              "type": "string",
              "const": "rect"
            }
          },
          "additionalProperties": true,
          "required": [
            "kind"
          ]
        },
        {
          "description": "An ellipse inscribed in the transform box.",
          "type": "object",
          "properties": {
            "kind": {
              "type": "string",
              "const": "ellipse"
            }
          },
          "additionalProperties": true,
          "required": [
            "kind"
          ]
        },
        {
          "description": "A straight segment across the middle of the transform box, from the\nleft edge to the right.\n\nThe box *is* the line: `width` is its length, `rotation` is its angle,\nand `height` is layout only — nothing is drawn above or below the\nmiddle. A second endpoint in the payload would be a second way of\nsaying where a layer is, and `move`, `resize` and `rotate` would then\nhave to mean something different for this layer kind than for the\nother four (maintainer's decision, 2026-09-09).",
          "type": "object",
          "properties": {
            "markerStart": {
              "description": "What sits at the start of the segment, or `None`.\n\n**Markers are part of the Line payload, never preset-carried**:\nthey describe this segment's ends, not a style, and a preset that\ncarried them would move another line's arrows (Contents A6).",
              "anyOf": [
                {
                  "$ref": "#/$defs/LineMarker"
                },
                {
                  "type": "null"
                }
              ]
            },
            "markerEnd": {
              "description": "What sits at the end of the segment, or `None`. See\n`marker_start`: part of the payload, never preset-carried.",
              "anyOf": [
                {
                  "$ref": "#/$defs/LineMarker"
                },
                {
                  "type": "null"
                }
              ]
            },
            "kind": {
              "type": "string",
              "const": "line"
            }
          },
          "additionalProperties": true,
          "required": [
            "kind"
          ]
        },
        {
          "description": "A closed silhouette drawn from path data, filling the transform box.\n\nThe `d` string follows the conservative grammar of [`crate::path`] —\nno quadratics, one leading moveto, closed, bounded length and command\ncount — and is validated at **operation time**, so a `d` that fails\nthe grammar cannot enter a document through this build.",
          "type": "object",
          "properties": {
            "d": {
              "description": "The path data, as written in an SVG `d` attribute. Validated\nagainst [`crate::path`] at operation time.",
              "type": "string"
            },
            "kind": {
              "type": "string",
              "const": "path"
            }
          },
          "required": [
            "kind",
            "d"
          ],
          "additionalProperties": true
        },
        {
          "description": "A geometry this build does not know, preserved as written and refused\nwhen something tries to change or draw it — the same bargain as\n[`Effect::Other`] and [`BlendMode::Other`] (D21)."
        }
      ]
    },
    "LineMarker": {
      "description": "What sits at an end of a [`ShapeKind::Line`].\n\nA fixed, named set — never arbitrary marker markup: a marker's geometry\nis part of the engine, deterministic on every target, not part of the\ndocument. A name written by a newer build round-trips verbatim in\n[`LineMarker::Other`] and is refused when something tries to draw it.",
      "anyOf": [
        {
          "description": "No marker: the line just ends.",
          "type": "string",
          "const": "none"
        },
        {
          "description": "An open arrowhead pointing along the line.",
          "type": "string",
          "const": "arrow"
        },
        {
          "description": "A small disc centred on the endpoint.",
          "type": "string",
          "const": "circle"
        },
        {
          "description": "A marker name this build does not know, preserved verbatim (D21)."
        }
      ]
    },
    "ShapeLayer": {
      "description": "A primitive drawn from the document rather than from an imported file.\n\nThe transform box *is* the geometry: a rect fills it, an ellipse is\ninscribed in it, a line runs across its middle. Nothing here carries\ncoordinates of its own, so `move`, `resize` and `rotate` mean for a shape\nexactly what they already mean for every other layer, and layout bounds\nstay the box (D5).",
      "type": "object",
      "properties": {
        "shape": {
          "description": "The geometry, a nested object tagged by `kind`.\n\nNested rather than flattened on purpose. [`ShapeKind::Other`] is an\nuntagged catch-all, and an untagged variant flattened into this struct\nwould swallow `fill`, `stroke` and every unknown sibling key on its way\npast — the layer would round-trip, but the paint would have moved\ninside the geometry. One level of nesting keeps the catch-all's reach\nto the thing it is a catch-all for.",
          "$ref": "#/$defs/ShapeKind"
        },
        "fill": {
          "description": "Interior paint. `None` means no fill at all — SVG's `fill=\"none\"`,\nnot black.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color"
            },
            {
              "type": "null"
            }
          ]
        },
        "stroke": {
          "description": "Edge paint. `None` means no stroke.\n\nA shape with neither fill nor stroke is a valid document. Invisible is\nallowed here for the same reason `opacity: 0` is, and refusing it would\nmean an editor could not clear one paint before choosing the other.",
          "anyOf": [
            {
              "$ref": "#/$defs/Stroke"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "shape"
      ],
      "additionalProperties": true
    },
    "Preset": {
      "description": "A named bundle of style properties.",
      "type": "object",
      "properties": {
        "name": {
          "description": "What it is called. Unique within a document.",
          "type": "string"
        },
        "description": {
          "description": "What it is for, for whoever is choosing between presets — including an\nagent, which is the case that needs it most.",
          "type": [
            "string",
            "null"
          ]
        },
        "properties": {
          "description": "The properties it sets. Anything left out is left alone on apply.",
          "$ref": "#/$defs/PresetProperties"
        }
      },
      "required": [
        "name",
        "properties"
      ],
      "additionalProperties": true
    },
    "PresetProperties": {
      "description": "What a preset sets.\n\nEvery field optional, and absent means \"leave alone\" — so a preset that\nonly names a colour is a colour preset, and applying it does not quietly\nreset a layer's font.\n\nDeliberately no transform: a style is not a position. A preset that moved\nlayers would be a template, and templates already exist.",
      "type": "object",
      "properties": {
        "fontFamily": {
          "description": "Text layers: font family.",
          "type": [
            "string",
            "null"
          ]
        },
        "fontSize": {
          "description": "Text layers: font size.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "color": {
          "description": "Text layers: fill colour.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color"
            },
            {
              "type": "null"
            }
          ]
        },
        "align": {
          "description": "Text layers: horizontal alignment.",
          "anyOf": [
            {
              "$ref": "#/$defs/TextAlign"
            },
            {
              "type": "null"
            }
          ]
        },
        "lineHeight": {
          "description": "Text layers: line height.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "fontWeight": {
          "description": "Text layers: font weight, 100–900.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "fontStyle": {
          "description": "Text layers: upright or italic.",
          "anyOf": [
            {
              "$ref": "#/$defs/FontStyle"
            },
            {
              "type": "null"
            }
          ]
        },
        "letterSpacing": {
          "description": "Text layers: letter spacing, in pixels.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "verticalAlign": {
          "description": "Text layers: vertical alignment in the box.",
          "anyOf": [
            {
              "$ref": "#/$defs/VerticalAlign"
            },
            {
              "type": "null"
            }
          ]
        },
        "opacity": {
          "description": "Any layer: opacity.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "blendMode": {
          "description": "Any layer: how it composites.",
          "anyOf": [
            {
              "$ref": "#/$defs/BlendMode"
            },
            {
              "type": "null"
            }
          ]
        },
        "effects": {
          "description": "Any layer: the whole effect stack.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/Effect"
          }
        },
        "fill": {
          "description": "Shape layers: interior paint.\n\nA plain `Option`, unlike the update's doubly-optional `fill`: a preset\nthat omits it leaves the fill alone, and there is no spelling that\nclears one. A style says what a thing looks like; \"looks like nothing\"\nis not a house style, and a preset that could silently empty a shape\nwould be a sharp edge on a feature whose whole point is repeatability.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color"
            },
            {
              "type": "null"
            }
          ]
        },
        "stroke": {
          "description": "Shape layers: edge paint. See [`PresetProperties::fill`] for why it\ncannot be cleared.",
          "anyOf": [
            {
              "$ref": "#/$defs/Stroke"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": true
    },
    "Slot": {
      "description": "A named opening in a template.",
      "type": "object",
      "properties": {
        "name": {
          "description": "What a caller names this slot by. Unique within a document.",
          "type": "string"
        },
        "layer": {
          "description": "The layer it fills.",
          "$ref": "#/$defs/LayerId"
        },
        "kind": {
          "description": "What may be supplied for it.",
          "$ref": "#/$defs/SlotKind",
          "default": "text"
        },
        "description": {
          "description": "What this slot is for, for whoever is filling it — including an agent,\nwhich is the case that needs it most.",
          "type": [
            "string",
            "null"
          ]
        },
        "required": {
          "description": "Whether a variant must supply it.",
          "type": "boolean",
          "default": false
        }
      },
      "required": [
        "name",
        "layer"
      ],
      "additionalProperties": true
    },
    "SlotKind": {
      "description": "What a slot lets a caller supply.",
      "oneOf": [
        {
          "description": "The text of a text layer.",
          "type": "string",
          "const": "text"
        },
        {
          "description": "The asset an image layer draws.",
          "type": "string",
          "const": "image"
        },
        {
          "description": "The fill colour of a text layer, or of a shape layer.\n\nOne slot kind for both, resolved by the layer it points at (D8): a\ntext layer's `color` and a shape layer's `fill` are the same question\nasked of two payloads, and a template that wanted a brand colour in\nboth a headline and the badge behind it should not need two slot\nkinds to say so. A shape's *stroke* colour is not slot-able — a stroke\nis a colour and a width together, and half of one is not a value.",
          "type": "string",
          "const": "color"
        }
      ]
    }
  }
}
